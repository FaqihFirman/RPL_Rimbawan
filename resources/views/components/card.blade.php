<div class="max-w-xs rounded overflow-hidden shadow-lg bg-white">
    <img class="w-full h-60 object-cover" src="{{ $imageUrl }}" alt="{{ $namaUmum }}">
    <div class="px-4 py-3">
        <div class="font-bold text-lg mb-1">{{ $namaUmum }}</div>
        <p class="text-gray-600 italic text-sm">{{ $namaLatin }}</p>
    </div>
</div>
